[build]
  command = "npm ci"
  publish = "."

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["express", "mercadopago"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true
